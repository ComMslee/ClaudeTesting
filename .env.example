# ─── Telegram 알림 설정 ──────────────────────────────────────────────────────
# 1. Telegram에서 @BotFather → /newbot 으로 봇 생성 후 토큰 복사
# 2. 봇에게 메시지 전송 후 아래 URL에서 chat.id 확인:
#    https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHAT_ID=123456789

# ─── 수원시 도서관 로그인 정보 ────────────────────────────────────────────────
SUWON_USERNAME=your_library_member_id
SUWON_PASSWORD=your_password

# ─── 예약 희망 정보 ──────────────────────────────────────────────────────────
# 캠핑을 원하는 날짜 (YYYY-MM-DD 형식)
CAMPING_DATE=2026-03-15
# 희망 구역명 (사이트에 표시되는 <option> 텍스트와 정확히 일치해야 함)
CAMPSITE_NAME=A구역
# 이용 인원
ATTENDEE_COUNT=4

# ─── 재시도 / 타이밍 설정 (기본값으로 충분, 변경 불필요) ─────────────────────
# 예약 실패 시 최대 재시도 횟수
MAX_RETRIES=10
# 재시도 간격 (초)
RETRY_DELAY_SECONDS=1
# 10시 몇 초 전에 브라우저를 시작하고 로그인할지 (기본 30초)
PRE_POSITION_SECONDS=30
# true = 헤드리스(화면 없음), false = 브라우저 창 표시
# 첫 실행 시 false 권장 — WAF 우회에도 도움이 됨
HEADLESS=false

# ─── 경로 설정 ───────────────────────────────────────────────────────────────
# 컨테이너 내부 스크린샷 저장 경로 (docker-compose.yml 볼륨과 연결됨)
SCREENSHOT_DIR=/app/screenshots
